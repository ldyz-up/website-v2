# 阿里云上线 · 小白详细版（照着做就行）

> 您的目标：国内外客户用手机扫名片上的二维码，就能打开公司官网；客户在网站上填询价表，信息自动发到您的邮箱。
> 已确定方案：**阿里云轻量应用服务器（海外节点·新加坡）+ 宝塔面板 + PHP 收件脚本**。
> 客户分布：约 20% 国内、80% 海外（欧洲、日韩）→ 选**新加坡**节点：海外客户访问快，**无需 ICP 备案**，买完即可上线。
> 预计花费：第一年约 **¥170~220 左右**（服务器 ¥99~150 + 域名 ¥70，邮箱和 SSL 免费）。

---

## 购买清单与费用（先看这个）

| 项目 | 买什么 | 大概价格 | 说明 |
|------|--------|----------|------|
| 域名 | 您已在阿里云买好 `.com` 域名 | 约 ¥60~90/年 | 已完成实名认证即可 |
| 服务器 | 轻量应用服务器 **2核2G（新加坡/东京节点）** | 新用户约 **¥99~150/年**（以官网活动价为准） | 海外客户访问快，免备案 |
| 企业邮箱 | 阿里云企业邮箱**免费版** | ¥0 | 用来给客户发回复、收询盘 |
| SSL 证书 | 宝塔面板一键申请（Let's Encrypt） | ¥0 | 网址变成 https，带小锁 |
| ICP 备案 | **不需要**（海外节点免备案） | ¥0 | 买完当天即可上线 |
| **合计** | | **约 ¥170~220/年** | 第二年服务器续费约 ¥300~600，可趁活动续多年 |

> 为什么选**海外节点**？80% 客户在欧洲、日韩：大陆服务器海外访问慢且要备案；海外节点海外快、免备案。
> 为什么选**新加坡**？欧洲、日韩、国内三方均衡；若日韩客户最多可改选**日本东京**，若更看重国内速度可改选**香港**。
> 为什么选 2核2G？活动价与低配差价小，更流畅、以后加产品也不卡。

---

## 第 1 步：买服务器（约 10 分钟）

1. 打开阿里云官网 → 搜索框输入 **「轻量应用服务器」** → 进入产品页。
2. 点 **「立即购买」**，按下表填写：

| 选项 | 怎么选 | 原因 |
|------|--------|------|
| 地域 | **新加坡**（推荐）｜备选：日本东京、香港 | 80% 海外客户访问快，且免备案 |
| 套餐规格 | **2核2G、3M 带宽、60GB SSD** | 流量小够用，性价比最高 |
| 镜像/系统 | **Ubuntu 22.04**（或 24.04） | 宝塔面板支持最好 |
| 时长 | **1 年**（有多年活动就买多年） | 锁定便宜价格，避免第二年涨价 |
| 登录方式 | 选 **「密码登录」** | 对小白最简单 |

3. 下单付款后，进入 **「轻量应用服务器控制台」**，找到您的服务器，**抄下两个信息**（后面一直要用）：
   - **公网 IP**（形如 `47.xxx.xxx.xxx`）
   - **root 密码**（如果没设置，在服务器详情里点「重置密码」设置一个，记到备忘录）

---

## 第 2 步：放行端口（约 2 分钟）

1. 在轻量服务器控制台 → 点击服务器 → 左侧菜单 **「防火墙」**（轻量服务器没有“安全组”，端口在“防火墙”这里放行）。
2. 点 **「添加规则」**，依次放行下面 4 个端口：

| 端口 | 协议 | 用途 |
|------|------|------|
| 22 | TCP | SSH 远程连接（默认已放行，检查一下） |
| 80 | TCP | 网站 http 访问 |
| 443 | TCP | 网站 https 访问 |
| 8888 | TCP | 宝塔面板登录（安装完先开，全部弄好后建议关掉） |

---

## 第 3 步：无需备案（海外节点，省 1~2 周）

> 选**新加坡/东京/香港**等海外节点的服务器，**不需要 ICP 备案**，买完当天就能上线。
> 只有大陆服务器才强制备案。以后如果想把网站迁回大陆服务器，再补备案即可（到时候找我，我把步骤给您）。

**现在直接跳到第 4 步**（域名解析）→ 第 5 步（企业邮箱）→ 第 6 步（装宝塔）→……

---

## 第 4 步：域名解析（买好服务器即可做，约 10 分钟）

1. 阿里云控制台搜索 **「云解析 DNS」** → 找到您的域名 → 点 **「解析设置」**。
2. 点 **「添加记录」**，添加两条 A 记录（值都填第 1 步抄的公网 IP）：

| 记录类型 | 主机记录 | 记录值 | 其他 |
|----------|----------|--------|------|
| A | `@` | 您的公网 IP | TTL 默认 600 |
| A | `www` | 您的公网 IP | TTL 默认 600 |

3. 保存后一般 10 分钟~2 小时生效。**效果**：输入 `www.您的域名` 能访问服务器。

> 海外节点无需备案，买好服务器、解析好域名，网站当天就能上线。

---

## 第 5 步：开通企微邮箱 + 获取 SMTP 授权密码（约 20 分钟）

> 这一步是让「客户在网站提交询盘 → 自动发信到您邮箱」的关键。

1. 电脑浏览器打开 **https://exmail.qq.com**（企业微信邮箱 / 腾讯企业邮箱），用管理员或账号管理员身份登录；若还没有邮箱，先在企业微信管理后台开通「邮箱」并创建账号 `sales@longdeyizhi.com`。
2. 按提示把域名解析（MX / SPF 等记录）指向腾讯（阿里云域名控制台 → 解析设置，腾讯会给出具体记录值）。
3. 用 `sales@longdeyizhi.com` 登录**网页版邮箱** → 找到 **「设置 → 收发信设置 → 客户端设置」** → 开启 **「SMTP 服务」** → 按提示**生成客户端授权密码**（⚠️ 这是一串专用密码，不是登录密码，只显示一次，抄下来）。
4. 把下面 3 个信息记到备忘录（第 8 步填 `send_mail.php` 要用）：

```
SMTP 服务器：smtp.exmail.qq.com
SMTP 端口：465（SSL）
发件邮箱：sales@longdeyizhi.com
客户端授权密码：刚才生成的那串
```

> 说明：`send_mail.php` 里 SMTP 已默认填好企微邮箱参数，您只需要把「客户端授权密码」填进 `smtp_pass` 即可。

---

## 第 6 步：服务器安装宝塔面板（约 15 分钟）

> 宝塔是网页版服务器管理工具，装好后在浏览器里点鼠标就能上传网站、绑定域名、申请证书，不用记命令。

1. **打开“终端”App**（Mac 在“启动台”搜索“终端”；Windows 用阿里云网页版的「远程连接」功能或 Xshell）。
2. 输入下面的命令连接服务器（把 `47.xxx.xxx.xxx` 换成您的公网 IP，回车后会提示输入密码）：

```bash
ssh root@47.xxx.xxx.xxx
```

3. 登录成功后，到宝塔官网 **https://www.bt.cn** → 下载页 → 复制 **Linux 版 / Ubuntu** 的一键安装命令，粘贴到终端回车（约 5~10 分钟）。也可以直接粘贴这一条（以官网为准）：

```bash
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh
```

4. 安装完成后，终端会显示一串信息，**全部抄下来**：
   - 面板地址（形如 `http://公网IP:8888/随机字符`）
   - 用户名（默认 `admin`）
   - 密码（随机生成）
5. 浏览器打开面板地址 → 登录 → 首次会弹窗让您选环境，选 **LNMP**（Nginx + MySQL + PHP）：
   - PHP 版本选 **7.4 或 8.x**
   - 其他默认即可，点「一键安装」，等它装完（约 10~20 分钟）。

---

## 第 7 步：上传网站文件（约 10 分钟）

1. 宝塔面板左侧 → **「文件」** → 进入目录 `/www/wwwroot` → 右上角 **「新建文件夹」**，命名为 `您的域名`（如 `longdeyizhi.com`）。
2. 点进入该文件夹 → 点 **「上传」** → 把本地 `公司官网 ai 工作文件夹` 里的**这些内容**拖进去（**不是**上传整个文件夹，是它里面的这些）：

```
index.html
css/           （整个文件夹）
js/            （整个文件夹）
images/        （整个文件夹）
send_mail.php
test_mail.php
```

3. 上传完成后，在文件列表里**确认 index.html 就在这个目录下**（不是嵌套一层）。

---

## 第 8 步：配置收件脚本 + 测试收信（约 10 分钟）

1. 宝塔「文件」里找到 `send_mail.php` → 右键 → **「编辑」** → 找到顶部「配置区」，按第 5 步记下的信息修改：

```php
'to_email'    => 'sales@您的域名',   // 收询盘的邮箱
'from_email'  => 'sales@您的域名',   // 发件邮箱（和第5步一致）
'smtp_user'   => 'sales@您的域名',
'smtp_pass'   => '把客户端授权密码填这里',
```

2. 保存后，浏览器打开：`http://公网IP/test_mail.php`
3. 点 **「发送测试邮件」** → 去邮箱收信：
   - **收到测试邮件** = 配置成功 ✅
   - **报错** = 多半是 smtp_pass 填成了登录密码，或账号不一致，对照第 5 步改。
4. 测试成功后，把 `test_mail.php` **删除**（防止别人乱用）。

---

## 第 9 步：创建网站 + 绑定域名 + 免费 HTTPS（约 10 分钟）

1. 宝塔左侧 → **「网站」** → 点 **「添加站点」**：

| 选项 | 怎么填 |
|------|--------|
| 域名 | `您的域名` 和 `www.您的域名` 都填（用空格或换行分开） |
| 根目录 | 自动填到 `/www/wwwroot/您的域名`（确认无误） |
| PHP 版本 | 选 7.4 或 8.x |
| 数据库 / FTP | 不需要，都选「不创建」 |

2. 添加完成后，点该站点右侧 **「设置」** → **「SSL」**：
   - 选 **Let's Encrypt** → 勾选两个域名 → 点申请 → 成功后打开 **「强制 HTTPS」**。
3. 等第 4 步的域名解析生效后（一般 10 分钟~2 小时），浏览器打开 `https://www.您的域名`：
   - 能看到官网 = 成功 🎉
   - 打不开 = 先确认解析是否生效（`https://tool.chinaz.com/dns` 查一下 A 记录 IP 是否等于服务器公网 IP），再确认防火墙 80/443 已放行。

---

## 第 10 步：测试上线清单（约 15 分钟）

- [ ] 手机 + 电脑都能打开 `https://www.您的域名`（地址栏有小锁）
- [ ] 手机网络（关掉 Wi-Fi 用 4G/5G）也能打开 —— 模拟客户访问
- [ ] 6 种语言（EN/中/繁/日/한/FR）都能切换
- [ ] 提交一次询价表单 → 邮箱收到询盘 ✅
- [ ] 服务器上 `/www/wwwroot/../inquiries/inquiries.csv`（即 `/www/wwwroot/inquiries/`）有备份记录
- [ ] 页面上电话、邮箱、地址点击可用

**名片二维码：**
1. 打开免费二维码工具（如「草料二维码」cli.im，或 Google 搜“二维码生成器”）；
2. 内容填 `https://www.您的域名`；
3. 生成后下载 **PNG 图片**，排版名片时放到名片背面/角落，建议尺寸 **25mm × 25mm** 以上；
4. 有二维码图片后也可以发给我，我帮您放到官网联系区。

---

## 常见问题

**问：海外节点不用备案，靠谱吗？** 靠谱。备案只针对中国大陆服务器；海外节点（新加坡/东京/香港）合法免备案，全球客户都能正常访问。

**问：测试邮件发不出去？** ① smtp_pass 必须用「客户端授权密码」不是登录密码；② SMTP 服务器/端口确认是 `smtp.qiye.aliyun.com:465`；③ 企业邮箱里确认已开启 SMTP 服务。

**问：域名打不开？** 依次检查：解析是否生效（A 记录 IP 是否等于服务器公网 IP）→ 宝塔站点是否创建成功 → 防火墙 80/443 是否放行。

**问：国内 20% 客户访问新加坡服务器会慢吗？** 基本可用（延迟一般几十到一百多毫秒，打开网页感受不到明显差异）；若以后国内客户变多，可换香港节点或加国内 CDN，随时找我。

**问：服务器到期忘了续费？** 网站会直接打不开，建议在阿里云开启「自动续费」，并把续费提醒设好。

---

## 费用回顾

- 第一年：服务器 ¥99~150（新加坡/东京活动价）+ 域名 ¥70 ≈ **¥170~220**
- 第二年起：服务器续费约 ¥300~600（趁活动续多年更划算）+ 域名 ¥80 ≈ **¥380~680/年**
- 全程无其他隐藏费用（邮箱、SSL 免费；海外节点免备案）
